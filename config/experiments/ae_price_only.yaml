{
  "experiment_name": "ae_price_only",
  "features": {
    "enabled": [
      "price_volatility",
      "price_momentum",
      "price_liquidity"
    ]
  },
  "assembly": {
    "dataset_name": "ae_price_only",
    "required_feature_groups": [
      "price_volatility",
      "price_momentum",
      "price_liquidity"
    ]
  },
  "model": {
    "name": "autoencoder",
    "embedding_dim": 8,
    "hidden_dim": 32,
    "learning_rate": 0.001,
    "epochs": 40,
    "batch_size": 512,
    "weight_decay": 0.00001
  },
  "evaluation": {
    "enabled": [
      "clustering",
      "peers"
    ]
  }
}
